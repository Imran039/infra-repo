steps:
  - name: "hashicorp/terraform:latest"
    entrypoint: "bash"
    args:
      - "-c"
      - "chmod +x terraform/terraform-deploy.sh && ./terraform/terraform-deploy.sh"

options:
  logging: CLOUD_LOGGING_ONLY
